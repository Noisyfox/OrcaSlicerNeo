// Script file (no imports/exports) — declares the '@orca/slicer-app-css'
// alias used by both hosts' side-effect import of the shared stylesheet.
// Host tsconfigs include this package's src, so the ambient declaration
// applies to their compilations without per-host copies.
declare module '@orca/slicer-app-css' {}
